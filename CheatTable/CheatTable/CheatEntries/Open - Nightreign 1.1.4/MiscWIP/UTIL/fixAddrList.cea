{$lua}
if syntaxcheck then return end
[enable]
local addr_list = getAddressList()
local count = addr_list.getCount() - 1
for i = 0, count do
    local record = addr_list[i]
    if record then
        record.ID = i
    end
end
disableMemrec(memrec)
[disable]
